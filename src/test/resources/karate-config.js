function fun() {

   var config = {
       baseURL: 'https://jsonplaceholder.typicode.com/',
       bHook: 'BeforeHook is the first statement being executed before every scenario'
    }

    return config;
}